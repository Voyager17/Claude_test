{
  "permissions": {
    "allow": [
      "Bash(uv --version)",
      "Bash(uv venv)",
      "Bash(uv sync --all-extras)",
      "Bash(uv sync)",
      "Bash(uv run python scripts/seed.py)"
    ]
  }
}
